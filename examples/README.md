# Examples

`ambi-vox-lpc/` contains synthetic phrase-frame files for testing the Ambi Vox
Encoder loader. These examples are distinct from the VOT wavetable library in
`wavetables/`.

See `ambi-vox-lpc/README.md` for the frame mapping, included phrases, and
regeneration command.
