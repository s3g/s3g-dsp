We are continuing s3g-dsp testing on Windows 10, Intel Core i7-4510U @ 2.00 GHz,
8 GB RAM, using REAPER. Windows 11 is not our tested baseline.

Read WINDOWS_TESTING_HANDOFF.md completely before taking task actions. The
parent folder has SOURCE_SNAPSHOT.json and the exact Windows test packages.
Source baseline: main, 892e0af704b5316fce6b1c388f96f6c49ed2389b; this is a
source snapshot without .git, plus new handoff/packaging files. Verify transfer
hashes before edits. Do not assume the old Mac chat or build caches are present.
Before making source revisions, also read WINDOWS_CODE_INTEGRATION.md and use
its fresh-clone, short-lived branch and reviewed integration workflow.

First help me run the supplied Tracker integration checker from an ordinary
interactive Windows desktop and verify the existing page-visibility correction
in REAPER before rebuilding. The old bug hid embedded pages behind the shell;
floating/reattaching exposed detachable pages, but Tracker/Song stayed blank.
The fix and stronger checker are in the current packages, but native acceptance
is pending. Test first open and single-click all ten pages BEFORE any detachment,
then 65-200% outer resizing, hide/show, recall and multiple instances. Preserve
the exact binary path/hash and full logs. A mock-host pass is not REAPER acceptance.

Second help measure Point and Pyrosphere slow GUI loading, sluggish graphics
and dropouts. Lower point/voice count and ambisonic order help. Record the missing
REAPER/OS/driver/audio/display details; compare editor open vs closed without
bypass, then buffer/order/point-count changes one at a time. Separate real-time
CPU, drawing and paging before claiming a RAM minimum. This issue is not fixed.

Preserve the original Cocoa GUI features and accepted VSTGUI behavior, bundled
fonts/fallbacks, custom menus, keyboard focus, project formats and host-timed MIDI.
Tracker is CLAP-only; do not restore its retired standalone app. Ambi Energy is
excluded from the 121-plugin suite and is outside this task.

Begin with read-only setup checks and testing guidance; do not make speculative
source fixes before reproducing and explaining a remaining failure. Ask before
installation/replacement, commits/pushes, external transfer or security changes.
Never overwrite a loaded plug-in. Keep a change log and a patchable baseline for
returning any approved fixes to Mac. Follow native Windows build instructions,
not the Mac MinGW cross-build packaging scripts. Do not claim Windows, audio or
GUI tests were run if only source inspection or cross-compilation was performed.
