# User Tools

This directory contains optional utilities used by documented user workflows.

- `voicebank_builder.py` converts a prepared recording and timing manifest into
  an s3g voicebank. See `examples/voicebank-builder/README.md` for the command,
  input format, and generated files.

The plug-in build, test suite, package, and installer do not run this script.
